---
- name: restart sshd
  systemd:
    name: ssh
    state: restarted

- name: netplan apply and restart resolved
  shell: netplan apply && systemctl restart systemd-resolved
  listen: netplan apply and restart resolved
