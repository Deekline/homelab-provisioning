# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!terraform.tfvars.example
!*secrets.tfvars

# Ansible — vault.yml must be ansible-vault encrypted before committing
# Use vault.yml.example as the template
ansible/inventory/group_vars/all/vault.yml
